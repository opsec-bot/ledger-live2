---
"ledger-live-desktop": minor
---

Add an opt-in "Flex Mode" display setting (Settings > Accounts) that shows large, made-up portfolio and BTC/ETH/SOL/LTC balances for fun screenshots, instead of real balances. It only affects display formatting on the Portfolio dashboard and asset overview screens, never Send/Receive/Swap or transaction data, and always shows a persistent "Flex Mode" banner while active so it's never mistaken for real holdings.
